int myFunction(String a, String b) {
    return 3 - 1;
}

int a = myFunction("a", "b");

boolean t = true || false;
boolean f = 1 >= 2;

if (t && f) {
    int a = 12;
} else {
    String b = "test";
}

Range range = ['b','c'] + ['w'-'z'];
State state = $"Erster";
Transition transition = state - - range - - > $"End"^a;

return <$"Start"^1, { transition }>;
